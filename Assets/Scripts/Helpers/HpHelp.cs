using UnityEngine;

namespace Helpers
{
    public class HpHelp : MonoBehaviour
    {
        private void Update()
        {
            transform.rotation = Camera.main.transform.rotation;
        }
    }
}
